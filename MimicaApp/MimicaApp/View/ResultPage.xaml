<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MimicaApp.View.ResultPage">
    <ContentPage.Content>
        <StackLayout Padding="20">

            <Label Text="{Binding WinnerTeam.Name}"
                   Margin="0, 60, 0, 0"
                   FontSize="54"
                   HorizontalOptions="CenterAndExpand"
                   TextColor="LightGreen"/>

            <Label Text="WON!" 
                   HorizontalOptions="CenterAndExpand"
                   FontSize="36"/>

            <StackLayout Orientation="Horizontal">
                <Label Text="{Binding WinnerTeam.Score}" HorizontalOptions="EndAndExpand"/>
                <Label Text="points" HorizontalOptions="StartAndExpand"/>
            </StackLayout>

            <Label Text="{Binding LoserTeam.Name}"
                   FontSize="20"
                   Margin="0, 30, 0, 0"/>

            <StackLayout Orientation="Horizontal">
                <Label Text="{Binding LoserTeam.Score}"/>
                <Label Text="points"/>
            </StackLayout>

            <Button Margin="0, 30" Text="Play again" Command="{Binding PlayAgain}"/>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>